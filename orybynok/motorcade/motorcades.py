class Motorcades:
    def __ini__(self, number_of_car):
        self.number_of_car = number_of_car
        car = []
        truck = []
        bass = []
        traktor = []
        number_of_car = len(car) + len(truck) + len(bass) + len(traktor)